/**
 * La page racine / est gérée par le middleware next-intl.
 * Elle redirige automatiquement vers /fr (locale par défaut).
 * Ce fichier ne sera jamais affiché.
 */
export default function RootPage() {
  return null
}
